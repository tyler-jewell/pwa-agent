[package]
name = "msa-web"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Pure-Rust HTTP server for MSA agent HTML chat UI"

[lib]
name = "msa_web"
path = "src/lib.rs"

[[bin]]
name = "msa-web"
path = "src/main.rs"

[dependencies]
msa-ui = { path = "../msa-ui" }
dirs = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
which = { workspace = true }

[dev-dependencies]
# tests use in-process handle_request + loopback backend

[lints]
workspace = true
