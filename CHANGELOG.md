# Changelog

## Unreleased

- **Steady-foot continuous path:** claim–code audit tests; injectable live poll storage; deploy-surface + autonomous self-improve cycle tests; README bounds “infinity” as continuous Vercel deploy + bounded honest agent loops.
- **Army of small smart people (FP13–16):** capability/context snapshots, structured handoff, bounded task loop, honest achieved/not-achieved reports; `crew-agent` on bus; chat `task:` / multi-item goals; pure modules under `public/js/task/`.
- **Progressive model ladder** (PWA metaphor): pure `router/progress.js` stages shell → instant → enhancing → capable → offline-ready; sequential mock tiny→small→medium auto-load; large via recruiter; network-aware resume on `online`; rank prefers offlineReady when offline; Models UI shows stage + quality + offline badge.
- **performance-manager**: background per-refresh; MEMORY “Areas of opportunity” + progress; ≤1 promote-to-instructions notify via shared agent notify; Standing instructions on approve.
- **Recruiter** agent (background, one recommendation per refresh) + aggression lever; **trainer** downloads/benchmarks on approve; shared **agent notify** facade; live models roster + README markers.
- Rebrand product to **Progressive Web Agent** (`pwa`): README day-one goals (progressive models, MEMORY self-improve, create agents, transparent feed); package/UI/export/SW identity strings.
- Full-load dismiss-only banner: register UPDATE listener before check + explicit show from check result; push index uses Redis SADD/SREM when available.
- Soft-reset Update always cache-bust navigates (loads new JS); markUpdated only after successful softApplied load; SW SKIP_WAITING message handler; banner hide on Update; compact nested under turn root; per-endpoint push KV keys; chat-agent non-blocking reflect integration self-test.
- Review fixes: atomic export/import (single IDB tx + deep validate + memoryQueue), restore via memory-agent/bus, bus maps `ok:false` → tree error, router lesson failures pill, head repair, push store KV-optional, notify JSON 400, poll notify-once, dismiss-only banner copy, local-watch `public/**`, folder handle persist, purity self-tests without folder bind.
- Initial public-complete vertical slice: mock runtime, chat-agent + memory/router subagents, durable MEMORY + transcript, progressive models, quality gate, live-update poll/soft-reset/banner, export/import/reset, Vercel static + optional Web Push API stubs, offline shell SW.
