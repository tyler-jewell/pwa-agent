---
name: msa-agent-ui
description: >
  RETIRED for day-2. Use msa-ui-design + crates/msa-ui + crates/msa-web.
  Kept only so old grants do not point agents at deleted msa-chat.
---

# msa-agent-ui (retired)

Day-2 human UI is **`msa web`** (`msa-ui` + `msa-web`). Do **not** edit or reintroduce `crates/msa-chat` / egui.

Load **`msa-ui-design`** instead.
