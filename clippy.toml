# Clippy configuration for msa / admin-agent standard.
# Most policy is in workspace [lints] in Cargo.toml (deny-by-default).
cognitive-complexity-threshold = 30
too-many-arguments-threshold = 8
type-complexity-threshold = 250
